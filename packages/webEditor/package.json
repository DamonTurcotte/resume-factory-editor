{
  "name": "resume-factory-editor-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/editorHtml.ts",
  "scripts": {
    "start": "vite --host",
    "build": "tsc && vite build && node ./scripts/postBuild.js ./dist/index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "vite-plugin-singlefile": "^2.0.2"
  }
}
